## Hermit font
execute this cmd to set hermit font in the termux 
```bash
mkdir -p ~/.termux && curl -L "https://github.com/jonjeexe/hermit_font/raw/refs/heads/main/hermit.ttf" -o ~/.termux/font.ttf && termux-reload-settings
```
